## README

dictionary path : /usr/share/dict/american-english

example code: cat /usr/share/dict/american-english | grep computer | wc -l


tar and zip
tar -c <folder>			: group file into one folder
gzip -c > <zipfilename.tar.gz>	: zip file
gzip -d <zipfile>		: decompress
